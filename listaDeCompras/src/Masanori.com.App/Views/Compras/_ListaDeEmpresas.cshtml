@model Masanori.com.App.ViewModels.CompraViewComponent

@{ 
    var teste = ViewBag;

    var t = teste;
}

@if (Model != null)
{

    <form asp-action="AtualizarEmpresa">
        <div class="form-group">
            <input type="hidden" asp-for="EmpresaId" />
            <input type="hidden" asp-for="Id" />
        </div>
        <div class="form-group">
            <label asp-for="Id" class="control-label"></label>
            <select asp-for="Id" class="form-control" asp-items="@(new SelectList(Model.Empresas, "Id", "Nome"))">
                <option></option>
            </select>
        </div>
    </form>
}
